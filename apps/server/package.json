{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "build-binary": "bun build ./.output/server/index.mjs --compile --outfile ./binary/server"
  },
  "devDependencies": {
    "nitropack": "latest"
  }
}
